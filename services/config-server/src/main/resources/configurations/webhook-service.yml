server:
  port: 8999
spring:
  data:
    mongodb:
      uri:  mongodb://najim:najim@localhost:27017/webhook_db?authSource=admin

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      default-zone: http://${eureka.instance.hostname}:8761/eureka


